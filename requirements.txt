# Python dependencies
# Install: py -m pip install -r requirements.txt

# Google APIs
google-auth>=2.0.0
google-auth-oauthlib>=0.4.0
google-auth-httplib2>=0.1.0
google-api-python-client>=2.0.0

# Gemini AI
google-genai>=1.0.0

# Firebase (replaces SQLite + FastAPI dashboard)
firebase-admin>=6.0.0

# Data / scraping (existing)
pandas>=1.3.0
requests>=2.25.0
beautifulsoup4>=4.9.0
ddgs>=6.0.0
